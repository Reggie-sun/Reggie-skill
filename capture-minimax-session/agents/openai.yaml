interface:
  display_name: "Capture MiniMax Session"
  short_description: "Capture sanitized MiniMax subagent session diagnostics"
  default_prompt: "Use $capture-minimax-session to capture sanitized MiniMax subagent activity from session <session-id>, including an abnormal current sub-agents invocation."
policy:
  allow_implicit_invocation: true
